decisions = {
	change_years = {
		ai = no
		ai_check_interval = 10000
		
		potential = {
			is_multiplayer_host_character = yes
			has_game_rule = {
				name = when_roulette
				value = years
			}
		}
		
		allow = {
			NOT = { has_character_modifier = change_years_cooldown }
		}
		
		effect = {
			character_event = { id = ROULETTE.101 }
			hidden_effect = {
				add_character_modifier = {
					name = change_years_cooldown
					months = 1
					hidden = yes
					inherit = yes
				}
			}
		}
	}
	spin_the_wheel = {
		ai = no
		ai_check_interval = 10000
		
		potential = {
			ai = no
		}
		
		allow = {
			NOT = { has_character_modifier = spin_cooldown }
		}
		
		effect = {
			character_event = { id = ROULETTE.201 }
			hidden_effect = {
				add_character_modifier = {
					name = spin_cooldown
					months = 1
					hidden = yes
					inherit = yes
				}
			}
		}
	}
}